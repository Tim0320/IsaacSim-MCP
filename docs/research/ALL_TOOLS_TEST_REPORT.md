# Isaac Sim 6.0.1：128 tools 統一證據報告

本報告逐項聚合已完成的 guarded live verifier 證據。產生器本身只做 source inventory 與 read-only live snapshot，不會清除或改寫 Stage。

## 狀態定義

- `pass`：guarded live invocation has a successful postcondition/read-back
- `partial`：supported path passed, but a declared output or postcondition remains unverified
- `blocked`：invocation cannot proceed because an explicit external/runtime prerequisite is absent
- `unsupported`：the active runtime or backend explicitly rejects the capability
- `fail`：invocation reached the implementation and exposed a code or contract defect

## 本次 runtime snapshot

- Isaac Sim：`6.0.1-rc.7`
- Adapter / backend：`IsaacAdapterV6` / `physx`
- Extension commands：`128`
- Timeline / stage：`stopped` / ``
- 結果：`128` tools；blocked=11、pass=117

## 逐項證據

| Tool | 用途 | 前置條件 / input | Read-back | 結果 | 證據與限制 |
|---|---|---|---|---|---|
| `apply_material` | Bind a material to an object. | `(material_path*, target_prim_path*, material_purpose)`; Isaac Sim 6.0.1, TCP 8766, dedicated scratch namespace for writes | material schema, direct/resolved binding and physics behavior | **pass** | `scripts/verify_physics_material_live.py` (2026-08-24)；PhysX evidence only; Newton remains separately untested or unsupported unless capability data says otherwise. |
| `apply_stage_batch` | Preview or atomically apply up to 100 layer/arc/variant/semantic/attribute edits. Any failed operation restores the stage snapshot. | `(operations*, preview, readback_root_path)`; Isaac Sim 6.0.1, TCP 8766, dedicated scratch namespace for writes | layer stack, composition arcs, typed values, semantics, rollback and cleanup | **pass** | `scripts/verify_stage_composition_live.py` (2026-08-25)；PhysX evidence only; Newton remains separately untested or unsupported unless capability data says otherwise. |
| `bake_navmesh` | Bake the current stage NavMesh with a bounded wait; preview by default. | `(max_frames, preview)`; Isaac Sim 6.0.1, TCP 8766, dedicated scratch namespace for writes | ownership, behavior task, measured movement, navmesh and cleanup | **pass** | `scripts/verify_human_lifecycle_live.py` (2026-08-25)；PhysX evidence only; Newton remains separately untested or unsupported unless capability data says otherwise. |
| `cancel_job` | Request cancellation and return a predictable terminal lifecycle state. | `(job_id*)`; Isaac Sim 6.0.1, TCP 8766, dedicated scratch namespace for writes | retained job state, reconnect result, cancellation and cleanup | **pass** | `scripts/verify_job_diagnostics_live.py` (2026-08-25)；PhysX evidence only; Newton remains separately untested or unsupported unless capability data says otherwise. |
| `cancel_motion` | Cancel a running motion job and hold its last commanded position. | `(job_id*)`; Isaac Sim 6.0.1, TCP 8766, dedicated scratch namespace for writes | IK error, trajectory validation, progress, terminal state and cleanup | **pass** | `scripts/verify_motion_control_live.py` (2026-08-24)；PhysX evidence only; Newton remains separately untested or unsupported unless capability data says otherwise. |
| `cancel_sdg_job` | Preview or request cancellation; resources detach at the next safe point. | `(job_id*, preview)`; Isaac Sim 6.0.1, TCP 8766, dedicated scratch namespace for writes | job state, frame manifest, deterministic trace hash and cleanup | **pass** | `scripts/verify_replicator_sdg_live.py` (2026-08-25)；PhysX evidence only; Newton remains separately untested or unsupported unless capability data says otherwise. |
| `capture_camera_output` | Capture a typed RTX camera annotator output. | `(prim_path, output_type, output_path, return_mode, inline_max_bytes)`; Isaac Sim 6.0.1, TCP 8766, dedicated scratch namespace for writes | seven annotator arrays, semantic geometry, calibration matrices and SHA-256 | **pass** | `scripts/verify_camera_outputs_live.py` (2026-08-23)；PhysX evidence only; Newton remains separately untested or unsupported unless capability data says otherwise. |
| `capture_image` | Capture an RGB image from a camera sensor. | `(prim_path, output_path, return_mode, inline_max_bytes)`; Isaac Sim 6.0.1, TCP 8766, dedicated scratch namespace for writes | pixel shape/content, PNG bytes, raw and artifact SHA-256 | **pass** | `scripts/verify_camera_rgb_live.py` (2026-08-23)；PhysX evidence only; Newton remains separately untested or unsupported unless capability data says otherwise. |
| `cleanup_artifacts` | Delete expired managed artifacts and report reclaimed capacity. | `(無)`; Isaac Sim 6.0.1, TCP 8766, dedicated scratch namespace for writes | metadata, bounded chunks, full SHA-256, delete and expiry | **pass** | `scripts/verify_artifact_transport_live.py` (2026-08-23)；PhysX evidence only; Newton remains separately untested or unsupported unless capability data says otherwise. |
| `clear_scene` | Remove all prims from the scene. | `(keep_physics, keep_environment)`; Isaac Sim 6.0.1, TCP 8766, dedicated scratch namespace for writes | stage path, prim inventory, authored fixture or cleanup postcondition | **pass** | `docs/research/ALL_TOOLS_TEST_REPORT_2026-08-20_42_TOOLS.md` (2026-08-20)；PhysX evidence only; Newton remains separately untested or unsupported unless capability data says otherwise. |
| `clone_object` | Duplicate an existing object to a new prim path. | `(source_path*, target_path*, position)`; Isaac Sim 6.0.1, TCP 8766, dedicated scratch namespace for writes | exact prim path, transform and absence after delete | **pass** | `docs/research/ALL_TOOLS_TEST_REPORT_2026-08-20_42_TOOLS.md` (2026-08-20)；PhysX evidence only; Newton remains separately untested or unsupported unless capability data says otherwise. |
| `close_gripper` | Close a gripper to the profile's declared closed width with immediate target read-back. | `(prim_path*, profile*)`; Isaac Sim 6.0.1, TCP 8766, dedicated scratch namespace for writes | profile signature, command targets, measured velocity and stop targets | **pass** | `scripts/verify_controller_profiles_live.py` (2026-08-24)；PhysX evidence only; Newton remains separately untested or unsupported unless capability data says otherwise. |
| `compute_ik` | Solve bounded Lula inverse kinematics without moving the robot. | `(prim_path*, target_position*, end_effector_frame, target_orientation, seed_joint_positions, random_seed, position_tolerance, orientation_tolerance, max_iterations, timeout_ms, robot_model)`; Isaac Sim 6.0.1, TCP 8766, dedicated scratch namespace for writes | IK error, trajectory validation, progress, terminal state and cleanup | **pass** | `scripts/verify_motion_control_live.py` (2026-08-24)；PhysX evidence only; Newton remains separately untested or unsupported unless capability data says otherwise. |
| `configure_physics_body` | Atomically configure rigid/static body, collider approximation and mass. | `(prim_path*, body_type, collider_enabled, approximation, mass_kg, density_kg_m3)`; Isaac Sim 6.0.1, TCP 8766, dedicated scratch namespace for writes | typed USD schema, bodies, filters, joint frames/limits and rollback | **pass** | `scripts/verify_physics_authoring_live.py` (2026-08-24)；PhysX evidence only; Newton remains separately untested or unsupported unless capability data says otherwise. |
| `configure_script_node` | Preview or configure one exact ScriptNode in explicit inline/file mode. | `(graph_path*, node_path, mode, inline_script, script_file, preview)`; Isaac Sim 6.0.1, TCP 8766, dedicated scratch namespace for writes | nodes, edges, enabled/evaluation state, source hash and cleanup | **pass** | `scripts/verify_omnigraph_lifecycle_live.py` (2026-08-25)；PhysX evidence only; Newton remains separately untested or unsupported unless capability data says otherwise. |
| `connect_action_graph` | Preview or connect one source output to one target input in an Action Graph. | `(graph_path*, source_attr*, target_attr*, preview)`; Isaac Sim 6.0.1, TCP 8766, dedicated scratch namespace for writes | nodes, edges, enabled/evaluation state, source hash and cleanup | **pass** | `scripts/verify_omnigraph_lifecycle_live.py` (2026-08-25)；PhysX evidence only; Newton remains separately untested or unsupported unless capability data says otherwise. |
| `create_action_graph` | Create and wire an OmniGraph Action Graph. | `(graph_path, nodes, connections, values, evaluator, script_file, inline_script)`; Isaac Sim 6.0.1, TCP 8766, dedicated scratch namespace for writes | nodes, edges, enabled/evaluation state, source hash and cleanup | **pass** | `scripts/verify_omnigraph_lifecycle_live.py` (2026-08-25)；PhysX evidence only; Newton remains separately untested or unsupported unless capability data says otherwise. |
| `create_camera` | Add a camera sensor to the scene. | `(prim_path, position, rotation, resolution)`; Isaac Sim 6.0.1, TCP 8766, dedicated scratch namespace for writes | pixel shape/content, PNG bytes, raw and artifact SHA-256 | **pass** | `scripts/verify_camera_rgb_live.py` (2026-08-23)；PhysX evidence only; Newton remains separately untested or unsupported unless capability data says otherwise. |
| `create_collision_group` | Create a USD collision group and return relationship read-back. | `(group_path*, collider_paths*, filtered_group_paths, invert_filtered_groups, merge_group_name)`; Isaac Sim 6.0.1, TCP 8766, dedicated scratch namespace for writes | typed USD schema, bodies, filters, joint frames/limits and rollback | **pass** | `scripts/verify_physics_authoring_live.py` (2026-08-24)；PhysX evidence only; Newton remains separately untested or unsupported unless capability data says otherwise. |
| `create_lidar` | Add a lidar sensor to the scene. | `(prim_path, position, rotation, config, variant, horizontal_fov_deg, vertical_fov_deg, horizontal_resolution_deg, vertical_resolution_deg, rotation_rate_hz, min_range_m, max_range_m)`; Isaac Sim 6.0.1, TCP 8766, dedicated scratch namespace for writes | preset/generic RTX LiDAR USD schema values and invalid-config atomicity | **pass** | `scripts/verify_lidar_config_live.py` (2026-08-23)；PhysX evidence only; Newton remains separately untested or unsupported unless capability data says otherwise. |
| `create_light` | Create a light in the scene. | `(light_type, position, intensity, color, rotation, prim_path)`; Isaac Sim 6.0.1, TCP 8766, dedicated scratch namespace for writes | authored light prim and attributes | **pass** | `docs/research/ALL_TOOLS_TEST_REPORT_2026-08-20_42_TOOLS.md` (2026-08-20)；PhysX evidence only; Newton remains separately untested or unsupported unless capability data says otherwise. |
| `create_material` | Create a PBR or physics material. | `(material_type, prim_path, color, roughness, metallic, static_friction, dynamic_friction, restitution)`; Isaac Sim 6.0.1, TCP 8766, dedicated scratch namespace for writes | material schema, direct/resolved binding and physics behavior | **pass** | `scripts/verify_physics_material_live.py` (2026-08-24)；PhysX evidence only; Newton remains separately untested or unsupported unless capability data says otherwise. |
| `create_object` | Create a primitive object (Cube, Sphere, Cylinder, Cone, Capsule, Plane). | `(object_type, position, rotation, scale, size, color, physics_enabled, prim_path)`; Isaac Sim 6.0.1, TCP 8766, dedicated scratch namespace for writes | exact prim path, transform and absence after delete | **pass** | `docs/research/ALL_TOOLS_TEST_REPORT_2026-08-20_42_TOOLS.md` (2026-08-20)；PhysX evidence only; Newton remains separately untested or unsupported unless capability data says otherwise. |
| `create_physics_joint` | Create fixed, revolute, or prismatic joint with explicit local frames. | `(joint_path*, joint_type*, body1*, body0, axis, lower_limit, upper_limit, local_position0, local_rotation0, local_position1, local_rotation1, collision_enabled)`; Isaac Sim 6.0.1, TCP 8766, dedicated scratch namespace for writes | typed USD schema, bodies, filters, joint frames/limits and rollback | **pass** | `scripts/verify_physics_authoring_live.py` (2026-08-24)；PhysX evidence only; Newton remains separately untested or unsupported unless capability data says otherwise. |
| `create_physics_scene` | Create a physics scene with ground plane. Call get_scene_info first to verify connection. | `(gravity, scene_name)`; Isaac Sim 6.0.1, TCP 8766, dedicated scratch namespace for writes | stage path, prim inventory, authored fixture or cleanup postcondition | **pass** | `docs/research/ALL_TOOLS_TEST_REPORT_2026-08-20_42_TOOLS.md` (2026-08-20)；PhysX evidence only; Newton remains separately untested or unsupported unless capability data says otherwise. |
| `create_robot` | Create a robot in the scene from the Isaac Sim asset library. | `(robot_type, position, name, prim_path)`; Isaac Sim 6.0.1, TCP 8766, dedicated scratch namespace for writes | robot/DOF inventory, targets, measured state and atomic failure | **pass** | `scripts/verify_robot_joint_control_live.py` (2026-08-24)；PhysX evidence only; Newton remains separately untested or unsupported unless capability data says otherwise. |
| `create_ros2_camera_publisher` | Preview or create a ROS2CameraHelper publisher for an MCP camera runtime. | `(graph_path*, camera_prim_path*, render_product_path, topic_name, frame_id, camera_type, node_namespace, domain_id, qos_profile, use_system_time, preview)`; Isaac Sim 6.0.1, TCP 8766, dedicated scratch namespace for writes | workflow graph/prim plus external subscriber message and cleanup | **blocked** | `scripts/verify_ros2_workflows_live.py` (2026-08-25)；Previously live-verified with an external subscriber; current runtime extensions are disabled.；blocker=runtime_prerequisite: enabled Isaac Sim ROS 2 bridge/core/nodes |
| `create_ros2_clock_publisher` | Preview or create a playback-driven ROS 2 Clock publisher graph. | `(graph_path, topic_name, node_namespace, domain_id, qos_profile, reset_on_stop, preview)`; Isaac Sim 6.0.1, TCP 8766, dedicated scratch namespace for writes | workflow graph/prim plus external subscriber message and cleanup | **blocked** | `scripts/verify_ros2_workflows_live.py` (2026-08-25)；Previously live-verified with an external subscriber; current runtime extensions are disabled.；blocker=runtime_prerequisite: enabled Isaac Sim ROS 2 bridge/core/nodes |
| `create_ros2_joint_state_publisher` | Preview or create an IsaacReadJointState ROS 2 JointState publisher graph. | `(graph_path*, target_prim*, topic_name, node_namespace, domain_id, qos_profile, preview)`; Isaac Sim 6.0.1, TCP 8766, dedicated scratch namespace for writes | workflow graph/prim plus external subscriber message and cleanup | **blocked** | `scripts/verify_ros2_workflows_live.py` (2026-08-25)；Previously live-verified with an external subscriber; current runtime extensions are disabled.；blocker=runtime_prerequisite: enabled Isaac Sim ROS 2 bridge/core/nodes |
| `create_ros2_lidar_publisher` | Preview or create a ROS2RtxLidarHelper publisher for an MCP RTX LiDAR runtime. | `(graph_path*, lidar_prim_path*, render_product_path, topic_name, frame_id, lidar_type, node_namespace, domain_id, qos_profile, use_system_time, preview)`; Isaac Sim 6.0.1, TCP 8766, dedicated scratch namespace for writes | workflow graph/prim plus external subscriber message and cleanup | **blocked** | `scripts/verify_ros2_workflows_live.py` (2026-08-25)；Previously live-verified with an external subscriber; current runtime extensions are disabled.；blocker=runtime_prerequisite: enabled Isaac Sim ROS 2 bridge/core/nodes |
| `create_ros2_tf_publisher` | Preview or create a 6.0-style ComputeTransformTree to ROS 2 TF publisher. | `(graph_path*, target_prims*, parent_prim, topic_name, node_namespace, domain_id, qos_profile, static_publisher, preview)`; Isaac Sim 6.0.1, TCP 8766, dedicated scratch namespace for writes | workflow graph/prim plus external subscriber message and cleanup | **blocked** | `scripts/verify_ros2_workflows_live.py` (2026-08-25)；Previously live-verified with an external subscriber; current runtime extensions are disabled.；blocker=runtime_prerequisite: enabled Isaac Sim ROS 2 bridge/core/nodes |
| `create_sdg_job` | Preview or create a bounded manual-trigger BasicWriter SDG job. | `(camera_prim_path*, frame_count*, annotations*, resolution, seed, randomizers, rt_subframes, delta_time, preview)`; Isaac Sim 6.0.1, TCP 8766, dedicated scratch namespace for writes | job state, frame manifest, deterministic trace hash and cleanup | **pass** | `scripts/verify_replicator_sdg_live.py` (2026-08-25)；PhysX evidence only; Newton remains separately untested or unsupported unless capability data says otherwise. |
| `delete_action_graph` | Preview or delete one exact Action Graph, with deletion read-back. | `(graph_path*, preview)`; Isaac Sim 6.0.1, TCP 8766, dedicated scratch namespace for writes | nodes, edges, enabled/evaluation state, source hash and cleanup | **pass** | `scripts/verify_omnigraph_lifecycle_live.py` (2026-08-25)；PhysX evidence only; Newton remains separately untested or unsupported unless capability data says otherwise. |
| `delete_artifact` | Delete one managed artifact after validating its opaque handle. | `(handle*)`; Isaac Sim 6.0.1, TCP 8766, dedicated scratch namespace for writes | metadata, bounded chunks, full SHA-256, delete and expiry | **pass** | `scripts/verify_artifact_transport_live.py` (2026-08-23)；PhysX evidence only; Newton remains separately untested or unsupported unless capability data says otherwise. |
| `delete_human` | Delete one MCP-owned human. Preview is enabled by default. | `(human_path*, delete_empty_group, preview)`; Isaac Sim 6.0.1, TCP 8766, dedicated scratch namespace for writes | ownership, behavior task, measured movement, navmesh and cleanup | **pass** | `scripts/verify_human_lifecycle_live.py` (2026-08-25)；PhysX evidence only; Newton remains separately untested or unsupported unless capability data says otherwise. |
| `delete_object` | Delete an object; Camera/LiDAR use verified lifecycle teardown. | `(prim_path*, post_delete_updates)`; Isaac Sim 6.0.1, TCP 8766, dedicated scratch namespace for writes | exact prim path, transform and absence after delete | **pass** | `docs/research/ALL_TOOLS_TEST_REPORT_2026-08-20_42_TOOLS.md` (2026-08-20)；PhysX evidence only; Newton remains separately untested or unsupported unless capability data says otherwise. |
| `delete_ros2_workflow` | Preview or delete one MCP-owned ROS 2 workflow; foreign graphs are refused. | `(graph_path*, preview)`; Isaac Sim 6.0.1, TCP 8766, dedicated scratch namespace for writes | workflow graph/prim plus external subscriber message and cleanup | **blocked** | `scripts/verify_ros2_workflows_live.py` (2026-08-25)；Previously live-verified with an external subscriber; current runtime extensions are disabled.；blocker=runtime_prerequisite: enabled Isaac Sim ROS 2 bridge/core/nodes |
| `delete_sdg_job` | Preview or delete one terminal job record and optionally its artifacts. | `(job_id*, delete_artifacts, preview)`; Isaac Sim 6.0.1, TCP 8766, dedicated scratch namespace for writes | job state, frame manifest, deterministic trace hash and cleanup | **pass** | `scripts/verify_replicator_sdg_live.py` (2026-08-25)；PhysX evidence only; Newton remains separately untested or unsupported unless capability data says otherwise. |
| `delete_sensor` | Release and delete one Camera or LiDAR, then verify it stays absent. | `(prim_path*, post_delete_updates)`; Isaac Sim 6.0.1, TCP 8766, dedicated scratch namespace for writes | prim, runtime wrapper, render product, caches and metadata all absent | **pass** | `scripts/verify_sensor_lifecycle_live.py` (2026-08-23)；PhysX evidence only; Newton remains separately untested or unsupported unless capability data says otherwise. |
| `disconnect_action_graph` | Preview or remove one exact connection from an Action Graph. | `(graph_path*, source_attr*, target_attr*, preview)`; Isaac Sim 6.0.1, TCP 8766, dedicated scratch namespace for writes | nodes, edges, enabled/evaluation state, source hash and cleanup | **pass** | `scripts/verify_omnigraph_lifecycle_live.py` (2026-08-25)；PhysX evidence only; Newton remains separately untested or unsupported unless capability data says otherwise. |
| `edit_action_graph` | Edit an existing OmniGraph Action Graph: set attribute values or add connections. | `(graph_path, values, connections)`; Isaac Sim 6.0.1, TCP 8766, dedicated scratch namespace for writes | nodes, edges, enabled/evaluation state, source hash and cleanup | **pass** | `scripts/verify_omnigraph_lifecycle_live.py` (2026-08-25)；PhysX evidence only; Newton remains separately untested or unsupported unless capability data says otherwise. |
| `edit_composition_arc` | Preview or edit a reference/payload arc. Payloads also support load/unload; every mutation is read back and rolled back on failure. | `(prim_path*, arc_type*, action*, asset_path, target_prim_path, preview)`; Isaac Sim 6.0.1, TCP 8766, dedicated scratch namespace for writes | layer stack, composition arcs, typed values, semantics, rollback and cleanup | **pass** | `scripts/verify_stage_composition_live.py` (2026-08-25)；PhysX evidence only; Newton remains separately untested or unsupported unless capability data says otherwise. |
| `edit_sublayer` | Preview or add/remove a root-layer subLayer path with stopped-timeline and exact read-back checks. | `(action*, layer_path*, index, preview)`; Isaac Sim 6.0.1, TCP 8766, dedicated scratch namespace for writes | layer stack, composition arcs, typed values, semantics, rollback and cleanup | **pass** | `scripts/verify_stage_composition_live.py` (2026-08-25)；PhysX evidence only; Newton remains separately untested or unsupported unless capability data says otherwise. |
| `evaluate_action_graph` | Explicitly evaluate one exact Action Graph and return post-evaluation status. | `(graph_path*)`; Isaac Sim 6.0.1, TCP 8766, dedicated scratch namespace for writes | nodes, edges, enabled/evaluation state, source hash and cleanup | **pass** | `scripts/verify_omnigraph_lifecycle_live.py` (2026-08-25)；PhysX evidence only; Newton remains separately untested or unsupported unless capability data says otherwise. |
| `execute_script` | Escape hatch: execute arbitrary Python code in Isaac Sim. | `(code*, cwd, timeout_s, max_output_bytes, allow_background)`; Isaac Sim 6.0.1, TCP 8766, dedicated scratch namespace for writes | timeline/physics state, bounded script policy, audit and correlated logs | **pass** | `scripts/verify_command_governance_live.py` (2026-08-25)；PhysX evidence only; Newton remains separately untested or unsupported unless capability data says otherwise. |
| `execute_trajectory` | Start a trajectory job and return immediately without blocking the MCP worker. | `(trajectory_id*, timeout_ms)`; Isaac Sim 6.0.1, TCP 8766, dedicated scratch namespace for writes | IK error, trajectory validation, progress, terminal state and cleanup | **pass** | `scripts/verify_motion_control_live.py` (2026-08-24)；PhysX evidence only; Newton remains separately untested or unsupported unless capability data says otherwise. |
| `generate_3d` | Generate a 3D model from text or image using Beaver3D, then load it into the scene. | `(text_prompt, image_url, position, scale)`; Isaac Sim 6.0.1, TCP 8766, dedicated scratch namespace for writes | catalog or resulting USD prim | **blocked** | `docs/research/ALL_TOOLS_TEST_REPORT_2026-08-20_42_TOOLS.md` (2026-08-20)；Provider credentials/model configuration are external prerequisites, not a code defect.；blocker=external_configuration: ARK_API_KEY, BEAVER3D_MODEL |
| `get_action_graph` | Read one Action Graph's nodes, edges, state, and optional values or script source. | `(graph_path*, include_values, include_script_source)`; Isaac Sim 6.0.1, TCP 8766, dedicated scratch namespace for writes | nodes, edges, enabled/evaluation state, source hash and cleanup | **pass** | `scripts/verify_omnigraph_lifecycle_live.py` (2026-08-25)；PhysX evidence only; Newton remains separately untested or unsupported unless capability data says otherwise. |
| `get_action_graph_status` | Read one Action Graph's enabled, evaluation, and error status. | `(graph_path*)`; Isaac Sim 6.0.1, TCP 8766, dedicated scratch namespace for writes | nodes, edges, enabled/evaluation state, source hash and cleanup | **pass** | `scripts/verify_omnigraph_lifecycle_live.py` (2026-08-25)；PhysX evidence only; Newton remains separately untested or unsupported unless capability data says otherwise. |
| `get_artifact_info` | Get size, type, hash, expiry, and producer metadata for a managed artifact. | `(handle*)`; Isaac Sim 6.0.1, TCP 8766, dedicated scratch namespace for writes | metadata, bounded chunks, full SHA-256, delete and expiry | **pass** | `scripts/verify_artifact_transport_live.py` (2026-08-23)；PhysX evidence only; Newton remains separately untested or unsupported unless capability data says otherwise. |
| `get_camera_calibration` | Read camera intrinsics, extrinsics, projection, resolution, and units. | `(prim_path)`; Isaac Sim 6.0.1, TCP 8766, dedicated scratch namespace for writes | seven annotator arrays, semantic geometry, calibration matrices and SHA-256 | **pass** | `scripts/verify_camera_outputs_live.py` (2026-08-23)；PhysX evidence only; Newton remains separately untested or unsupported unless capability data says otherwise. |
| `get_capabilities` | Discover the live Isaac Sim MCP runtime and its explicit limitations. | `(無)`; Isaac Sim 6.0.1, TCP 8766, dedicated scratch namespace for writes | runtime, extensions, command registry and backend matrix | **pass** | `scripts/verify_backend_capability_matrix_live.py` (2026-08-24)；PhysX evidence only; Newton remains separately untested or unsupported unless capability data says otherwise. |
| `get_collision_group` | Read collision group members, filters, inversion and merge name. | `(group_path*)`; Isaac Sim 6.0.1, TCP 8766, dedicated scratch namespace for writes | typed USD schema, bodies, filters, joint frames/limits and rollback | **pass** | `scripts/verify_physics_authoring_live.py` (2026-08-24)；PhysX evidence only; Newton remains separately untested or unsupported unless capability data says otherwise. |
| `get_human` | Return USD and live Behavior Agent state for one IRA human. | `(human_path*)`; Isaac Sim 6.0.1, TCP 8766, dedicated scratch namespace for writes | ownership, behavior task, measured movement, navmesh and cleanup | **pass** | `scripts/verify_human_lifecycle_live.py` (2026-08-25)；PhysX evidence only; Newton remains separately untested or unsupported unless capability data says otherwise. |
| `get_isaac_logs` | Diagnostic tool: recent WARN/ERROR logs plus captured print() output. | `(clear, count, since_last_play, filter_command_id, severity, source)`; Isaac Sim 6.0.1, TCP 8766, dedicated scratch namespace for writes | correlated bounded structured records with credential redaction | **pass** | `scripts/verify_job_diagnostics_live.py` (2026-08-25)；PhysX evidence only; Newton remains separately untested or unsupported unless capability data says otherwise. |
| `get_job_status` | Read a managed, motion, or SDG job without executing it again. | `(job_id*)`; Isaac Sim 6.0.1, TCP 8766, dedicated scratch namespace for writes | retained job state, reconnect result, cancellation and cleanup | **pass** | `scripts/verify_job_diagnostics_live.py` (2026-08-25)；PhysX evidence only; Newton remains separately untested or unsupported unless capability data says otherwise. |
| `get_joint_config` | Diagnostic tool: get joint drive configuration for a robot articulation. | `(prim_path*)`; Isaac Sim 6.0.1, TCP 8766, dedicated scratch namespace for writes | timeline/physics state, bounded script policy, audit and correlated logs | **pass** | `scripts/verify_command_governance_live.py` (2026-08-25)；PhysX evidence only; Newton remains separately untested or unsupported unless capability data says otherwise. |
| `get_joint_positions` | Read current joint positions from a robot. | `(prim_path*)`; Isaac Sim 6.0.1, TCP 8766, dedicated scratch namespace for writes | robot/DOF inventory, targets, measured state and atomic failure | **pass** | `scripts/verify_robot_joint_control_live.py` (2026-08-24)；PhysX evidence only; Newton remains separately untested or unsupported unless capability data says otherwise. |
| `get_joint_state` | Read measured joint state and active command targets. | `(prim_path*, joint_names, joint_indices)`; Isaac Sim 6.0.1, TCP 8766, dedicated scratch namespace for writes | position/velocity/effort targets, measured values and atomic rejection | **pass** | `scripts/verify_robot_joint_control_live.py` (2026-08-24)；PhysX evidence only; Newton remains separately untested or unsupported unless capability data says otherwise. |
| `get_lidar_config` | Read effective LiDAR FOV, angular resolution, rate, range, and USD schema values. | `(prim_path)`; Isaac Sim 6.0.1, TCP 8766, dedicated scratch namespace for writes | preset/generic RTX LiDAR USD schema values and invalid-config atomicity | **pass** | `scripts/verify_lidar_config_live.py` (2026-08-23)；PhysX evidence only; Newton remains separately untested or unsupported unless capability data says otherwise. |
| `get_lidar_point_cloud` | Get typed point cloud fields from a lidar sensor. | `(prim_path, output_path, return_mode, inline_max_bytes)`; Isaac Sim 6.0.1, TCP 8766, dedicated scratch namespace for writes | typed Cartesian fields, known object ID mapping and NPZ SHA-256 | **pass** | `scripts/verify_lidar_point_cloud_live.py` (2026-08-23)；PhysX evidence only; Newton remains separately untested or unsupported unless capability data says otherwise. |
| `get_material` | Read PBR or physics material parameters and explicit units. | `(material_path*)`; Isaac Sim 6.0.1, TCP 8766, dedicated scratch namespace for writes | material schema, direct/resolved binding and physics behavior | **pass** | `scripts/verify_physics_material_live.py` (2026-08-24)；PhysX evidence only; Newton remains separately untested or unsupported unless capability data says otherwise. |
| `get_material_binding` | Read the resolved physics or visual material binding on a prim. | `(target_prim_path*, material_purpose)`; Isaac Sim 6.0.1, TCP 8766, dedicated scratch namespace for writes | material schema, direct/resolved binding and physics behavior | **pass** | `scripts/verify_physics_material_live.py` (2026-08-24)；PhysX evidence only; Newton remains separately untested or unsupported unless capability data says otherwise. |
| `get_motion_status` | Read motion job state, progress, timing, and terminal error details. | `(job_id*)`; Isaac Sim 6.0.1, TCP 8766, dedicated scratch namespace for writes | IK error, trajectory validation, progress, terminal state and cleanup | **pass** | `scripts/verify_motion_control_live.py` (2026-08-24)；PhysX evidence only; Newton remains separately untested or unsupported unless capability data says otherwise. |
| `get_navmesh_status` | Return Navigation Core, NavMeshVolume, bake and area status. | `(無)`; Isaac Sim 6.0.1, TCP 8766, dedicated scratch namespace for writes | ownership, behavior task, measured movement, navmesh and cleanup | **pass** | `scripts/verify_human_lifecycle_live.py` (2026-08-25)；PhysX evidence only; Newton remains separately untested or unsupported unless capability data says otherwise. |
| `get_physics_body` | Read authored rigid body, collider, approximation, mass and density. | `(prim_path*)`; Isaac Sim 6.0.1, TCP 8766, dedicated scratch namespace for writes | typed USD schema, bodies, filters, joint frames/limits and rollback | **pass** | `scripts/verify_physics_authoring_live.py` (2026-08-24)；PhysX evidence only; Newton remains separately untested or unsupported unless capability data says otherwise. |
| `get_physics_joint` | Read joint type, body targets, frames, axis, limits and units. | `(joint_path*)`; Isaac Sim 6.0.1, TCP 8766, dedicated scratch namespace for writes | typed USD schema, bodies, filters, joint frames/limits and rollback | **pass** | `scripts/verify_physics_authoring_live.py` (2026-08-24)；PhysX evidence only; Newton remains separately untested or unsupported unless capability data says otherwise. |
| `get_physics_state` | Diagnostic tool: get physics state for a prim. | `(prim_path*)`; Isaac Sim 6.0.1, TCP 8766, dedicated scratch namespace for writes | USD/runtime physics values, exact step timing and restored snapshot | **pass** | `scripts/verify_physics_params_live.py` (2026-08-24)；PhysX evidence only; Newton remains separately untested or unsupported unless capability data says otherwise. |
| `get_prim_info` | Get detailed information about a specific prim. | `(prim_path*)`; Isaac Sim 6.0.1, TCP 8766, dedicated scratch namespace for writes | stage path, prim inventory, authored fixture or cleanup postcondition | **pass** | `docs/research/ALL_TOOLS_TEST_REPORT_2026-08-20_42_TOOLS.md` (2026-08-20)；PhysX evidence only; Newton remains separately untested or unsupported unless capability data says otherwise. |
| `get_replicator_status` | Read Replicator extension, orchestrator, job, writer, and trigger state. | `(無)`; Isaac Sim 6.0.1, TCP 8766, dedicated scratch namespace for writes | job state, frame manifest, deterministic trace hash and cleanup | **pass** | `scripts/verify_replicator_sdg_live.py` (2026-08-25)；PhysX evidence only; Newton remains separately untested or unsupported unless capability data says otherwise. |
| `get_robot_info` | Get robot joint information including names, DOF count, joint types, and limits. | `(prim_path*)`; Isaac Sim 6.0.1, TCP 8766, dedicated scratch namespace for writes | robot/DOF inventory, targets, measured state and atomic failure | **pass** | `scripts/verify_robot_joint_control_live.py` (2026-08-24)；PhysX evidence only; Newton remains separately untested or unsupported unless capability data says otherwise. |
| `get_ros2_status` | Read ROS 2 bridge/core/nodes state, domain source, distro, and workflow count. | `(無)`; Isaac Sim 6.0.1, TCP 8766, dedicated scratch namespace for writes | workflow graph/prim plus external subscriber message and cleanup | **blocked** | `scripts/verify_ros2_workflows_live.py` (2026-08-25)；Previously live-verified with an external subscriber; current runtime extensions are disabled.；blocker=runtime_prerequisite: enabled Isaac Sim ROS 2 bridge/core/nodes |
| `get_scene_info` | Ping the Isaac Sim extension server and return scene information including stage path, assets root, and prim count. | `(無)`; Isaac Sim 6.0.1, TCP 8766, dedicated scratch namespace for writes | stage path, prim inventory, authored fixture or cleanup postcondition | **pass** | `docs/research/ALL_TOOLS_TEST_REPORT_2026-08-20_42_TOOLS.md` (2026-08-20)；PhysX evidence only; Newton remains separately untested or unsupported unless capability data says otherwise. |
| `get_script_audit_log` | Return bounded script audit metadata. Source code is represented only by SHA-256. | `(count)`; Isaac Sim 6.0.1, TCP 8766, dedicated scratch namespace for writes | timeline/physics state, bounded script policy, audit and correlated logs | **pass** | `scripts/verify_command_governance_live.py` (2026-08-25)；PhysX evidence only; Newton remains separately untested or unsupported unless capability data says otherwise. |
| `get_script_policy` | Return the current execute/reload policy without exposing script source or credentials. | `(無)`; Isaac Sim 6.0.1, TCP 8766, dedicated scratch namespace for writes | timeline/physics state, bounded script policy, audit and correlated logs | **pass** | `scripts/verify_command_governance_live.py` (2026-08-25)；PhysX evidence only; Newton remains separately untested or unsupported unless capability data says otherwise. |
| `get_sdg_job_status` | Read bounded SDG job progress, lifecycle state, and cleanup read-back. | `(job_id*)`; Isaac Sim 6.0.1, TCP 8766, dedicated scratch namespace for writes | job state, frame manifest, deterministic trace hash and cleanup | **pass** | `scripts/verify_replicator_sdg_live.py` (2026-08-25)；PhysX evidence only; Newton remains separately untested or unsupported unless capability data says otherwise. |
| `get_sdg_manifest` | Return a terminal job manifest and managed artifact handles. | `(job_id*)`; Isaac Sim 6.0.1, TCP 8766, dedicated scratch namespace for writes | job state, frame manifest, deterministic trace hash and cleanup | **pass** | `scripts/verify_replicator_sdg_live.py` (2026-08-25)；PhysX evidence only; Newton remains separately untested or unsupported unless capability data says otherwise. |
| `get_semantic_labels` | Read Isaac Sim 6.0.1 LabelsAPI taxonomies and labels, plus any legacy semantic labels. | `(prim_path*)`; Isaac Sim 6.0.1, TCP 8766, dedicated scratch namespace for writes | layer stack, composition arcs, typed values, semantics, rollback and cleanup | **pass** | `scripts/verify_stage_composition_live.py` (2026-08-25)；PhysX evidence only; Newton remains separately untested or unsupported unless capability data says otherwise. |
| `get_simulation_state` | Get the current simulation state: timeline status (playing/stopped/paused), | `(無)`; Isaac Sim 6.0.1, TCP 8766, dedicated scratch namespace for writes | timeline/physics state, bounded script policy, audit and correlated logs | **pass** | `scripts/verify_command_governance_live.py` (2026-08-25)；PhysX evidence only; Newton remains separately untested or unsupported unless capability data says otherwise. |
| `get_stage_composition` | Read the root layer, full layer stack, prim count, references/payloads, variant selections, semantics, and stage metadata. | `(root_path)`; Isaac Sim 6.0.1, TCP 8766, dedicated scratch namespace for writes | layer stack, composition arcs, typed values, semantics, rollback and cleanup | **pass** | `scripts/verify_stage_composition_live.py` (2026-08-25)；PhysX evidence only; Newton remains separately untested or unsupported unless capability data says otherwise. |
| `get_typed_attribute` | Read a USD attribute value, declared type, and authored-value state in JSON-safe form. | `(prim_path*, attribute*)`; Isaac Sim 6.0.1, TCP 8766, dedicated scratch namespace for writes | layer stack, composition arcs, typed values, semantics, rollback and cleanup | **pass** | `scripts/verify_stage_composition_live.py` (2026-08-25)；PhysX evidence only; Newton remains separately untested or unsupported unless capability data says otherwise. |
| `import_urdf` | Import a robot from a URDF file into the scene. | `(urdf_path*, prim_path, position)`; Isaac Sim 6.0.1, TCP 8766, dedicated scratch namespace for writes | catalog or resulting USD prim | **pass** | `docs/research/ALL_TOOLS_TEST_REPORT_2026-08-20_42_TOOLS.md` (2026-08-20)；PhysX evidence only; Newton remains separately untested or unsupported unless capability data says otherwise. |
| `list_action_graphs` | List Action Graphs below a USD root, including disabled graphs by default. | `(root_path, include_disabled)`; Isaac Sim 6.0.1, TCP 8766, dedicated scratch namespace for writes | nodes, edges, enabled/evaluation state, source hash and cleanup | **pass** | `scripts/verify_omnigraph_lifecycle_live.py` (2026-08-25)；PhysX evidence only; Newton remains separately untested or unsupported unless capability data says otherwise. |
| `list_available_robots` | List all available robots discovered from the Isaac Sim asset server. | `(無)`; Isaac Sim 6.0.1, TCP 8766, dedicated scratch namespace for writes | robot/DOF inventory, targets, measured state and atomic failure | **pass** | `scripts/verify_robot_joint_control_live.py` (2026-08-24)；PhysX evidence only; Newton remains separately untested or unsupported unless capability data says otherwise. |
| `list_controller_profiles` | List the exact gripper and mobile-base profiles accepted by high-level controller tools. | `(無)`; Isaac Sim 6.0.1, TCP 8766, dedicated scratch namespace for writes | profile signature, command targets, measured velocity and stop targets | **pass** | `scripts/verify_controller_profiles_live.py` (2026-08-24)；PhysX evidence only; Newton remains separately untested or unsupported unless capability data says otherwise. |
| `list_environments` | List all available environments discovered from the Isaac Sim asset server. | `(無)`; Isaac Sim 6.0.1, TCP 8766, dedicated scratch namespace for writes | stage path, prim inventory, authored fixture or cleanup postcondition | **pass** | `docs/research/ALL_TOOLS_TEST_REPORT_2026-08-20_42_TOOLS.md` (2026-08-20)；PhysX evidence only; Newton remains separately untested or unsupported unless capability data says otherwise. |
| `list_humans` | List IRA human characters without changing the stage. | `(root_prim_path, include_external)`; Isaac Sim 6.0.1, TCP 8766, dedicated scratch namespace for writes | ownership, behavior task, measured movement, navmesh and cleanup | **pass** | `scripts/verify_human_lifecycle_live.py` (2026-08-25)；PhysX evidence only; Newton remains separately untested or unsupported unless capability data says otherwise. |
| `list_jobs` | List the bounded retained managed-job registry. | `(count, include_terminal)`; Isaac Sim 6.0.1, TCP 8766, dedicated scratch namespace for writes | retained job state, reconnect result, cancellation and cleanup | **pass** | `scripts/verify_job_diagnostics_live.py` (2026-08-25)；PhysX evidence only; Newton remains separately untested or unsupported unless capability data says otherwise. |
| `list_nvidia_assets` | List NVIDIA assets that can be spawned through MCP. | `(category, query, max_results)`; Isaac Sim 6.0.1, TCP 8766, dedicated scratch namespace for writes | current NVIDIA asset catalog query succeeded and returned a bounded sample | **pass** | `scripts/generate_all_tools_report.py --live` (2026-08-25T16:28:11.755152+00:00)；PhysX evidence only; Newton remains separately untested or unsupported unless capability data says otherwise. |
| `list_prims` | List all prims in the scene, optionally filtered by type. | `(root_path, prim_type)`; Isaac Sim 6.0.1, TCP 8766, dedicated scratch namespace for writes | stage path, prim inventory, authored fixture or cleanup postcondition | **pass** | `docs/research/ALL_TOOLS_TEST_REPORT_2026-08-20_42_TOOLS.md` (2026-08-20)；PhysX evidence only; Newton remains separately untested or unsupported unless capability data says otherwise. |
| `list_ros2_workflows` | List MCP-owned ROS 2 publisher graphs below a USD root. | `(root_path)`; Isaac Sim 6.0.1, TCP 8766, dedicated scratch namespace for writes | workflow graph/prim plus external subscriber message and cleanup | **blocked** | `scripts/verify_ros2_workflows_live.py` (2026-08-25)；Previously live-verified with an external subscriber; current runtime extensions are disabled.；blocker=runtime_prerequisite: enabled Isaac Sim ROS 2 bridge/core/nodes |
| `load_environment` | Load a pre-built environment into the scene. Supports fuzzy matching. | `(environment*, prim_path)`; Isaac Sim 6.0.1, TCP 8766, dedicated scratch namespace for writes | stage path, prim inventory, authored fixture or cleanup postcondition | **pass** | `docs/research/ALL_TOOLS_TEST_REPORT_2026-08-20_42_TOOLS.md` (2026-08-20)；PhysX evidence only; Newton remains separately untested or unsupported unless capability data says otherwise. |
| `load_usd` | Load a USD asset from a URL or file path into the scene. | `(usd_url*, prim_path, position, scale)`; Isaac Sim 6.0.1, TCP 8766, dedicated scratch namespace for writes | catalog or resulting USD prim | **pass** | `docs/research/ALL_TOOLS_TEST_REPORT_2026-08-20_42_TOOLS.md` (2026-08-20)；PhysX evidence only; Newton remains separately untested or unsupported unless capability data says otherwise. |
| `modify_light` | Modify properties of an existing light. | `(prim_path*, intensity, color)`; Isaac Sim 6.0.1, TCP 8766, dedicated scratch namespace for writes | authored light prim and attributes | **pass** | `docs/research/ALL_TOOLS_TEST_REPORT_2026-08-20_42_TOOLS.md` (2026-08-20)；PhysX evidence only; Newton remains separately untested or unsupported unless capability data says otherwise. |
| `new_stage` | Preview or create a new empty stage. Requires scratch_stage=true, a scratch_root, stopped timeline, and defaults to preview-only. | `(scratch_stage, scratch_root, preview)`; Isaac Sim 6.0.1, TCP 8766, dedicated scratch namespace for writes | layer stack, composition arcs, typed values, semantics, rollback and cleanup | **pass** | `scripts/verify_stage_composition_live.py` (2026-08-25)；PhysX evidence only; Newton remains separately untested or unsupported unless capability data says otherwise. |
| `open_gripper` | Open a gripper to the profile's declared open width with immediate target read-back. | `(prim_path*, profile*)`; Isaac Sim 6.0.1, TCP 8766, dedicated scratch namespace for writes | profile signature, command targets, measured velocity and stop targets | **pass** | `scripts/verify_controller_profiles_live.py` (2026-08-24)；PhysX evidence only; Newton remains separately untested or unsupported unless capability data says otherwise. |
| `open_stage` | Preview or open a local USD stage inside scratch_root. Destructive stage replacement requires explicit scratch_stage=true. | `(path*, scratch_stage, scratch_root, preview, readback_root_path)`; Isaac Sim 6.0.1, TCP 8766, dedicated scratch namespace for writes | layer stack, composition arcs, typed values, semantics, rollback and cleanup | **pass** | `scripts/verify_stage_composition_live.py` (2026-08-25)；PhysX evidence only; Newton remains separately untested or unsupported unless capability data says otherwise. |
| `pause_simulation` | Pause the physics simulation. | `(無)`; Isaac Sim 6.0.1, TCP 8766, dedicated scratch namespace for writes | timeline/physics state, bounded script policy, audit and correlated logs | **pass** | `scripts/verify_command_governance_live.py` (2026-08-25)；PhysX evidence only; Newton remains separately untested or unsupported unless capability data says otherwise. |
| `plan_joint_trajectory` | Plan a bounded joint trajectory and return an opaque trajectory_id. | `(prim_path*, goal_joint_positions*, start_joint_positions, planner, random_seed, max_iterations, timeout_ms, robot_model)`; Isaac Sim 6.0.1, TCP 8766, dedicated scratch namespace for writes | IK error, trajectory validation, progress, terminal state and cleanup | **pass** | `scripts/verify_motion_control_live.py` (2026-08-24)；PhysX evidence only; Newton remains separately untested or unsupported unless capability data says otherwise. |
| `play_simulation` | Start the physics simulation. | `(無)`; Isaac Sim 6.0.1, TCP 8766, dedicated scratch namespace for writes | timeline/physics state, bounded script policy, audit and correlated logs | **pass** | `scripts/verify_command_governance_live.py` (2026-08-25)；PhysX evidence only; Newton remains separately untested or unsupported unless capability data says otherwise. |
| `read_artifact` | Read one bounded base64 chunk from a managed artifact. | `(handle*, offset, length)`; Isaac Sim 6.0.1, TCP 8766, dedicated scratch namespace for writes | metadata, bounded chunks, full SHA-256, delete and expiry | **pass** | `scripts/verify_artifact_transport_live.py` (2026-08-23)；PhysX evidence only; Newton remains separately untested or unsupported unless capability data says otherwise. |
| `refresh_robot_library` | Force re-scan the asset server for available robots. Use this if new robot assets were added. | `(無)`; Isaac Sim 6.0.1, TCP 8766, dedicated scratch namespace for writes | robot/DOF inventory, targets, measured state and atomic failure | **pass** | `scripts/verify_robot_joint_control_live.py` (2026-08-24)；PhysX evidence only; Newton remains separately untested or unsupported unless capability data says otherwise. |
| `reload_script` | Reload a Python controller from a file on disk. | `(file_path*, module_name, timeout_s, max_output_bytes)`; Isaac Sim 6.0.1, TCP 8766, dedicated scratch namespace for writes | timeline/physics state, bounded script policy, audit and correlated logs | **pass** | `scripts/verify_command_governance_live.py` (2026-08-25)；PhysX evidence only; Newton remains separately untested or unsupported unless capability data says otherwise. |
| `reload_script_node` | Preview or recompile one exact ScriptNode without cross-graph fallback. | `(graph_path*, node_path, mode, inline_script, script_file, preview)`; Isaac Sim 6.0.1, TCP 8766, dedicated scratch namespace for writes | nodes, edges, enabled/evaluation state, source hash and cleanup | **pass** | `scripts/verify_omnigraph_lifecycle_live.py` (2026-08-25)；PhysX evidence only; Newton remains separately untested or unsupported unless capability data says otherwise. |
| `save_stage_as` | Preview or export the current stage to a different local USD file inside scratch_root. Source overwrite is always rejected. | `(path*, scratch_stage, scratch_root, overwrite, preview, readback_root_path)`; Isaac Sim 6.0.1, TCP 8766, dedicated scratch namespace for writes | layer stack, composition arcs, typed values, semantics, rollback and cleanup | **pass** | `scripts/verify_stage_composition_live.py` (2026-08-25)；PhysX evidence only; Newton remains separately untested or unsupported unless capability data says otherwise. |
| `search_usd` | Search the NVIDIA USD asset library by text description, then load the best match. | `(text_prompt*, target_path, position, scale)`; Isaac Sim 6.0.1, TCP 8766, dedicated scratch namespace for writes | catalog or resulting USD prim | **blocked** | `docs/research/ALL_TOOLS_TEST_REPORT_2026-08-20_42_TOOLS.md` (2026-08-20)；No secret is requested, stored, or printed by this report.；blocker=external_configuration: NVIDIA_API_KEY |
| `set_action_graph_enabled` | Preview or enable/disable one Action Graph and read back its effective state. | `(graph_path*, enabled*, preview)`; Isaac Sim 6.0.1, TCP 8766, dedicated scratch namespace for writes | nodes, edges, enabled/evaluation state, source hash and cleanup | **pass** | `scripts/verify_omnigraph_lifecycle_live.py` (2026-08-25)；PhysX evidence only; Newton remains separately untested or unsupported unless capability data says otherwise. |
| `set_gripper_width` | Set total gripper opening width using an explicit robot profile. | `(prim_path*, profile*, width_m*)`; Isaac Sim 6.0.1, TCP 8766, dedicated scratch namespace for writes | profile signature, command targets, measured velocity and stop targets | **pass** | `scripts/verify_controller_profiles_live.py` (2026-08-24)；PhysX evidence only; Newton remains separately untested or unsupported unless capability data says otherwise. |
| `set_human_behavior` | Update supported live Behavior Agent settings with read-back. | `(human_path*, enabled, speed_mps, navigation_areas, obstacle_avoidance_enabled, auto_avoidance_enabled, preview)`; Isaac Sim 6.0.1, TCP 8766, dedicated scratch namespace for writes | ownership, behavior task, measured movement, navmesh and cleanup | **pass** | `scripts/verify_human_lifecycle_live.py` (2026-08-25)；PhysX evidence only; Newton remains separately untested or unsupported unless capability data says otherwise. |
| `set_human_idle` | Cancel the active action by issuing Idle, optionally facing one target. | `(human_path*, facing_position, facing_prim_path, preview)`; Isaac Sim 6.0.1, TCP 8766, dedicated scratch namespace for writes | ownership, behavior task, measured movement, navmesh and cleanup | **pass** | `scripts/verify_human_lifecycle_live.py` (2026-08-25)；PhysX evidence only; Newton remains separately untested or unsupported unless capability data says otherwise. |
| `set_human_look_at` | Issue a Behavior Agent LookAt task; zero duration means indefinite. | `(human_path*, target_position, target_prim_path, duration_seconds, preview)`; Isaac Sim 6.0.1, TCP 8766, dedicated scratch namespace for writes | ownership, behavior task, measured movement, navmesh and cleanup | **pass** | `scripts/verify_human_lifecycle_live.py` (2026-08-25)；PhysX evidence only; Newton remains separately untested or unsupported unless capability data says otherwise. |
| `set_human_target` | Issue a Behavior Agent MoveTo task to exactly one target. | `(human_path*, target_position, target_prim_path, speed_mps, auto_brake, preview)`; Isaac Sim 6.0.1, TCP 8766, dedicated scratch namespace for writes | ownership, behavior task, measured movement, navmesh and cleanup | **pass** | `scripts/verify_human_lifecycle_live.py` (2026-08-25)；PhysX evidence only; Newton remains separately untested or unsupported unless capability data says otherwise. |
| `set_joint_command` | Apply an atomic position, velocity, or effort joint command. | `(prim_path*, mode*, values*, joint_names, joint_indices)`; Isaac Sim 6.0.1, TCP 8766, dedicated scratch namespace for writes | position/velocity/effort targets, measured values and atomic rejection | **pass** | `scripts/verify_robot_joint_control_live.py` (2026-08-24)；PhysX evidence only; Newton remains separately untested or unsupported unless capability data says otherwise. |
| `set_joint_drive_config` | Atomically update drive gains, limits, or drive type for selected joints. | `(prim_path*, stiffness, damping, max_force, max_velocity, drive_type, joint_names, joint_indices)`; Isaac Sim 6.0.1, TCP 8766, dedicated scratch namespace for writes | drive gains, force/velocity limits, drive type and rollback | **pass** | `scripts/verify_robot_joint_drive_config_live.py` (2026-08-24)；PhysX evidence only; Newton remains separately untested or unsupported unless capability data says otherwise. |
| `set_joint_positions` | Set target joint positions on a robot via ArticulationAction. | `(prim_path*, joint_positions*, joint_indices)`; Isaac Sim 6.0.1, TCP 8766, dedicated scratch namespace for writes | robot/DOF inventory, targets, measured state and atomic failure | **pass** | `scripts/verify_robot_joint_control_live.py` (2026-08-24)；PhysX evidence only; Newton remains separately untested or unsupported unless capability data says otherwise. |
| `set_mobile_base_velocity` | Apply a profiled differential or holonomic base velocity target. | `(prim_path*, profile*, forward_mps*, lateral_mps, yaw_radps)`; Isaac Sim 6.0.1, TCP 8766, dedicated scratch namespace for writes | profile signature, command targets, measured velocity and stop targets | **pass** | `scripts/verify_controller_profiles_live.py` (2026-08-24)；PhysX evidence only; Newton remains separately untested or unsupported unless capability data says otherwise. |
| `set_physics_params` | Atomically configure Isaac Sim 6 PhysX scene parameters. | `(gravity, time_step, gpu_enabled)`; Isaac Sim 6.0.1, TCP 8766, dedicated scratch namespace for writes | USD/runtime physics values, exact step timing and restored snapshot | **pass** | `scripts/verify_physics_params_live.py` (2026-08-24)；PhysX evidence only; Newton remains separately untested or unsupported unless capability data says otherwise. |
| `set_semantic_labels` | Preview or apply Isaac Sim 6.0.1 UsdSemantics LabelsAPI labels under one taxonomy with read-back. | `(prim_path*, taxonomy*, labels*, overwrite, preview)`; Isaac Sim 6.0.1, TCP 8766, dedicated scratch namespace for writes | layer stack, composition arcs, typed values, semantics, rollback and cleanup | **pass** | `scripts/verify_stage_composition_live.py` (2026-08-25)；PhysX evidence only; Newton remains separately untested or unsupported unless capability data says otherwise. |
| `set_typed_attribute` | Preview or set a finite, explicitly typed USD attribute. Existing attributes require overwrite=true and cannot change type. | `(prim_path*, attribute*, type_name*, value*, custom, overwrite, preview)`; Isaac Sim 6.0.1, TCP 8766, dedicated scratch namespace for writes | layer stack, composition arcs, typed values, semantics, rollback and cleanup | **pass** | `scripts/verify_stage_composition_live.py` (2026-08-25)；PhysX evidence only; Newton remains separately untested or unsupported unless capability data says otherwise. |
| `set_variant_selection` | Preview or select an existing USD variant after validating the set and available names. | `(prim_path*, variant_set*, selection*, preview)`; Isaac Sim 6.0.1, TCP 8766, dedicated scratch namespace for writes | layer stack, composition arcs, typed values, semantics, rollback and cleanup | **pass** | `scripts/verify_stage_composition_live.py` (2026-08-25)；PhysX evidence only; Newton remains separately untested or unsupported unless capability data says otherwise. |
| `spawn_human` | Spawn animated NVIDIA human characters into the CURRENT USD stage. | `(count, group_name, root_prim_path, asset_path, motion_library_path, behavior, spawn_areas, speed_range, distance_range, idle_time_range, patrol_points, position, rotation, auto_create_navmesh_volume, navmesh_volume_center, navmesh_volume_size, seed)`; Isaac Sim 6.0.1, TCP 8766, dedicated scratch namespace for writes | ownership, behavior task, measured movement, navmesh and cleanup | **pass** | `scripts/verify_human_lifecycle_live.py` (2026-08-25)；PhysX evidence only; Newton remains separately untested or unsupported unless capability data says otherwise. |
| `spawn_nvidia_asset` | Spawn one catalog asset into the CURRENT USD stage. | `(asset_key*, prim_path, position, rotation, scale)`; Isaac Sim 6.0.1, TCP 8766, dedicated scratch namespace for writes | catalog or resulting USD prim | **blocked** | `docs/research/ALL_TOOLS_TEST_REPORT_2026-08-20_42_TOOLS.md` (2026-08-20)；Catalog/contract exists, but no preserved per-tool 6.0.1 scratch live postcondition is available.；blocker=runtime_prerequisite: dedicated exact-path scratch stage |
| `start_job` | Start an eligible asset or sensor command as a bounded background job. | `(command_type*, params, deadline_ms)`; Isaac Sim 6.0.1, TCP 8766, dedicated scratch namespace for writes | retained job state, reconnect result, cancellation and cleanup | **pass** | `scripts/verify_job_diagnostics_live.py` (2026-08-25)；PhysX evidence only; Newton remains separately untested or unsupported unless capability data says otherwise. |
| `start_sdg_job` | Preview or asynchronously start one configured SDG job. | `(job_id*, preview)`; Isaac Sim 6.0.1, TCP 8766, dedicated scratch namespace for writes | job state, frame manifest, deterministic trace hash and cleanup | **pass** | `scripts/verify_replicator_sdg_live.py` (2026-08-25)；PhysX evidence only; Newton remains separately untested or unsupported unless capability data says otherwise. |
| `step_simulation` | Advance the simulation by exactly N physics frames on a FROZEN timeline. | `(num_steps, observe_prims, observe_joints)`; Isaac Sim 6.0.1, TCP 8766, dedicated scratch namespace for writes | timeline/physics state, bounded script policy, audit and correlated logs | **pass** | `scripts/verify_command_governance_live.py` (2026-08-25)；PhysX evidence only; Newton remains separately untested or unsupported unless capability data says otherwise. |
| `stop_mobile_base` | Set every profiled wheel velocity target to zero and verify the stop target read-back. | `(prim_path*, profile*)`; Isaac Sim 6.0.1, TCP 8766, dedicated scratch namespace for writes | profile signature, command targets, measured velocity and stop targets | **pass** | `scripts/verify_controller_profiles_live.py` (2026-08-24)；PhysX evidence only; Newton remains separately untested or unsupported unless capability data says otherwise. |
| `stop_simulation` | Stop the physics simulation and reset to spawn state. | `(無)`; Isaac Sim 6.0.1, TCP 8766, dedicated scratch namespace for writes | timeline/physics state, bounded script policy, audit and correlated logs | **pass** | `scripts/verify_command_governance_live.py` (2026-08-25)；PhysX evidence only; Newton remains separately untested or unsupported unless capability data says otherwise. |
| `transform_object` | Set the transform (position, rotation, scale) of an existing object. | `(prim_path*, position, rotation, scale)`; Isaac Sim 6.0.1, TCP 8766, dedicated scratch namespace for writes | exact prim path, transform and absence after delete | **pass** | `docs/research/ALL_TOOLS_TEST_REPORT_2026-08-20_42_TOOLS.md` (2026-08-20)；PhysX evidence only; Newton remains separately untested or unsupported unless capability data says otherwise. |

## 重跑方式

```powershell
.\.venv\Scripts\python.exe .\scripts\generate_all_tools_report.py --live
.\.venv\Scripts\python.exe -m pytest -q tests\test_all_tools_report.py
```

Machine-readable artifact：[`ALL_TOOLS_TEST_RESULTS.json`](ALL_TOOLS_TEST_RESULTS.json)。`blocked` 與 `fail` 分開統計，外部金鑰不存在不會被誤報成程式缺陷。
